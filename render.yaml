services:
  - type: web
    name: crepuscule-online
    runtime: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm run serve
    healthCheckPath: /api/health
    autoDeploy: true
